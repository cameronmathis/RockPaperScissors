FROM mysql

ENV MYSQL_DATABASE rps_db
ENV MYSQL_ROOTPASSWORD "$MYSQL_ROOTPASSWORD"
ENV MYSQL_USER RPSManager
ENV MYSQL_PASSWORD "$MYSQL_RPSMANAGER_PASSWORD"
ENV DATABASE_HOST db

EXPOSE 3306

COPY ./mysql_scripts /docker-entrypoint-initdb.d/